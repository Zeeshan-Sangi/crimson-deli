rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App reads/writes through Next.js API + Admin SDK for now.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}