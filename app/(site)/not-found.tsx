import NotFoundContent from "@/components/site/NotFoundContent";

/** 404 for storefront routes — picks up the site chrome from (site)/layout.tsx. */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
